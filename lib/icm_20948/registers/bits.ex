defmodule Icm20948.Registers.Bits do
  # defmacro pwr_mgmt_1(), do: [1, 1, 1, 1, 1, 3]
  # defmacro lp_config(), do: [1, 1, 1, 1, 4]
  # defmacro accel_config(), do: [2, 3, 2,1]
  # defmacro gyro_config_1(), do: [2, 3, 2,1]
  # defmacro accel_smplrt_div_1(), do: [4,4]
  # defmacro accel_smplrt_div_2(), do: [8]
end
